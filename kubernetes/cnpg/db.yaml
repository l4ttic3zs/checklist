apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: checklist-database
spec:
  instances: 3
  monitoring:
    enablePodMonitor: true
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: checklist_db
      owner: db_user
